# Don't treat code/backticks & non-http tokens as links
exclude = [
  "CT:*",               # Civic Tome pseudo refs
  "mailto:*",
  "localhost",          # documented dev backend ports, not real links
  "127.0.0.1",
  "/tags/",             # GitHub release-tag refs, not local files
  "arxiv.org",          # Rate-limits CI link checkers
  "psypost.org",        # Aggregator, flaky
  "techxplore.com",     # Aggregator, flaky
  "digi-con.org"        # Academic site, intermittent
]
# Retry a couple times for GitHub flakiness
max_retries = 2
timeout = 20
# Accept 200..399
accept = [200, 204, 206, 301, 302, 307, 308]
# Consider GitHub relative links OK (they exist in repo)
require_https = false
# Skip bulk/imported content — chat dumps & inbox aren't authored docs and carry
# thousands of pseudo-links (same bulk-pollution fix as codespell/markdownlint).
exclude_path = [
  "docs/conversations",
  "docs/constella/_inbox",
  "docs/data",
  "docs/discussions",
  "review",              # dated review-scroll snapshots (archival cross-refs)
  "docs/tome"            # Civic Tome structural placeholders (unbuilt subsections)
]
